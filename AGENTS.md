# BPSR Chat Overlay

## プロジェクト概要

このプロジェクトは、BLUE PROTOCOL: Star Resonance のゲーム内チャットを
Npcapで読み取り、WPF画面に外部表示するWindowsアプリです。

ゲームへの入力、改造、自動操作は行いません。
通信を読み取るだけの読み取り専用ツールです。

## 開発者について

開発者はITエンジニアとして25年以上の経験がありますが、C#とWPFは初心者です。

一般的なシステム設計、アプリ開発、インフラ、上流工程の経験はあります。
そのため、機能を初心者向けに単純化する必要はありません。

ただし、C#、WPF、.NET固有の実装については、
変更理由と処理目的が理解できる説明を付けてください。

## 現在の開発方針

まず確実に動作させ、その後で整理します。

既存動作を壊さないことを最優先してください。
必要以上の抽象化や大規模リファクタリングは行わないでください。

以下は、明示的な依頼なしに導入しないでください。

- MVVMへの全面移行
- DIコンテナ
- 大量のインターフェース
- 新しいフレームワーク
- 大規模なフォルダ再編
- 通信処理の全面書き換え

改善案の提案は歓迎しますが、設計変更は実装前に確認してください。

## 現在動作している処理

以下の処理経路は動作確認済みです。

Npcap
→ TCP再構築
→ ゲームプロトコル解析
→ ChitChatNtf通知
→ ChatCaptureManager
→ ChatReceivedイベント
→ ObservableCollection
→ WPF ListBox表示

この経路を壊さないでください。

## コードの区分

BPSR_ZDPSLib 名前空間のコードは、既存ツールBPSR-ZDPSから移植した通信基盤です。

主な対象は次のとおりです。

- NetCap
- TcpReassembler
- ChatManager
- MessageManager

BPSRChatOverlay 名前空間のコードは、このアプリ独自のコードです。

ZDPS由来の警告は、現在は動作を優先しているため、
依頼がない限りまとめて修正しないでください。

## 名前空間

このプロジェクト独自コードの名前空間は、
BPSRChatOverlay に統一してください。

BPSR_ChatOverlay は使用しないでください。

BPSR_ZDPSLib と Zproto は変更しないでください。

## コード変更時のルール

コードを変更する前に、対象ファイルと方針を簡潔に説明してください。

変更後は、以下を報告してください。

1. 変更したファイル
2. 変更理由
3. 実装内容
4. 既存動作への影響
5. ビルド結果
6. 残っている警告または確認事項

コード変更後は可能な限りビルドしてください。

不明な仕様を推測で確定せず、必要な場合は質問してください。

UI表示に関する責務は、可能な限り Model ではなく
Converter や UIResources へ配置してください。

## Git運用

大きな変更を一度に行わず、レビュー可能な小さな単位で変更してください。

既存ファイルの削除、依存パッケージの追加、
プロジェクト設定の大幅変更は、実行前に確認してください。

## 作業範囲

各作業では、ユーザーから提示された最新の依頼内容を作業範囲とします。

依頼範囲外の大規模な変更やリファクタリングは行わず、
既存機能への影響を最小限にしてください。

作業前にGit状態とビルド結果を確認し、
作業後に変更内容とビルド結果を報告してください。
